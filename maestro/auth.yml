appId: com.nicolasobp.digitalnomadapp
---
- launchApp:
    clearState: true

- runFlow: sign-in.yml

# Sign out
- tapOn:
    text: .*Perfil.*

- repeat:
    while:
      notVisible: ".*Bem Vindo.*"
    commands:
      - tapOn:
          id: "sign-out-button"

- assertVisible: ".*Bem Vindo.*"
